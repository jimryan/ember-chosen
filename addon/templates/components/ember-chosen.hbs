<select size="1" id="ember-chosen-{{elementId}}" onchange={{action 'selectValue'}} multiple={{multiple}} disabled={{disabled}}>
  {{#if hasBlock}}
    {{yield}}
  {{else}}
    {{#unless skipEmptyItem}}
      <option></option>
    {{/unless}}

    {{#if invalidAttrs}}
      <option disabled>Invalid Attributes</option>
    {{else}}
      {{#if validGroupPath}}
        {{#each options as |group|}}
          <optgroup label={{group.label}}>
            {{#each group.options as |opt|}}
              <option value={{opt.value}} selected="{{if opt.selected 'selected'}}">{{opt.label}}</option>
            {{else}}
              <option disabled>No Options Available</option>
            {{/each}}
          </optgroup>
        {{else}}
          <option disabled>No Options Available</option>
        {{/each}}
      {{else}}
        {{#each options as |opt|}}
          <option value={{opt.value}} selected="{{if opt.selected 'selected'}}">{{opt.label}}</option>
        {{else}}
          <option disabled>No Options Available</option>
        {{/each}}
      {{/if}}
    {{/if}}
  {{/if}}
</select>
